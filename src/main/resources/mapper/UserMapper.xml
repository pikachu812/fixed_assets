<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fixed_assets.dao.UserDao">
    <insert id="addUser" parameterType="com.example.fixed_assets.entity.User">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="userId">
            select LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user (username, password, role) VALUES (#{username}, #{password}, #{role})
    </insert>

    <update id="updateUser" parameterType="com.example.fixed_assets.entity.User">
        UPDATE user SET username = #{username}, role = #{role} WHERE user_id = #{userId}
    </update>
    <update id="updateUserPassword" parameterType="com.example.fixed_assets.entity.User">
        UPDATE user SET password = #{password} WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>

    <select id="getUser" parameterType="int" resultType="com.example.fixed_assets.entity.User">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="findByUsername" resultType="com.example.fixed_assets.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <select id="searchUsers" resultType="com.example.fixed_assets.entity.User">
        SELECT * FROM user
        <where>
            <if test="username != null">
                OR username = #{username}
            </if>
            <if test="role != null">
                OR role = #{role}
            </if>
        </where>
    </select>
</mapper>
